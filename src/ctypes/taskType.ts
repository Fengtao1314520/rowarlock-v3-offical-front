export enum taskType {
  daily = "daily",
  temporary = "temporary",
  longTerm = "longTerm",
  shortTerm = "shortTerm",
  other = "other",
}

export enum taskStatus {
  todo = "todo",
  doing = "doing",
  done = "done",
  block = "block",
  cancel = "cancel",
}
